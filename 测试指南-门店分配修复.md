# 门店分配修复测试指南

## 问题说明

在之前的版本中，首次完成门店分配后，存在以下问题：
- ❌ 很多门店的每日汇总（各车型加起来）不等于设定的总量
- ❌ 需要手动调整一次才会自动平衡

## 修复内容

已重写 `store-allocation.js` 中的 `generateStoreData()` 函数（第394-512行）。

### 新的分配策略

**原逻辑问题**：
1. 先用最大余数法计算各车型的月度目标
2. 对每个车型，独立地用最大余数法按日分配
3. 独立计算理论的每日总量
4. 简单调整（会破坏车型月度总量守恒）

**新逻辑优化**：
1. ✅ 先计算门店的每日总量目标（使用最大余数法，确保总和=门店总目标）
2. ✅ 对每一天，按照各车型的区域占比，将该天的总量分配给各车型（使用最大余数法）
3. ✅ 确保每天各车型加起来 = 该天目标总量（严格相等）
4. ✅ 确保所有天加起来 = 门店月度总目标（严格相等）

## 如何测试

### 步骤1：正常使用工具

1. 打开 `index.html` 页面
2. 上传历史数据Excel文件
3. 选择目标年月
4. 点击"生成分配比例"
5. 为每个车型设置月度目标
6. 选择区域
7. 分配门店目标（可以上传Excel或手动输入）
8. 点击"确认分配并生成门店数据"

### 步骤2：运行自动测试

打开浏览器开发者工具（按F12），在控制台（Console）中输入：

```javascript
testStoreAllocation()
```

测试脚本会自动验证：
- ✅ 每天各车型加起来是否等于该天的目标总量
- ✅ 所有天的总量加起来是否等于门店月度总目标
- ✅ 各车型的月度总量汇总
- ✅ 所有值是否都是非负整数

### 步骤3：查看单个门店详情（可选）

如果需要查看某个门店的详细数据，在控制台输入：

```javascript
inspectStore("门店名称")
```

例如：
```javascript
inspectStore("北京祥云小镇")
```

这会显示该门店每天每个车型的分配详情。

## 预期测试结果

### 成功示例

```
========================================
🧪 开始测试门店数据分配
========================================

📊 测试配置：
   区域：北京战区
   门店数量：20
   车型数量：3
   车型列表：L6, L7, L9

============================================================
🏪 测试门店 [1/20]: 北京祥云小镇
============================================================
   月度目标：500
   天数：30

✅ 测试1: 验证每天各车型加起来 = 该天的目标总量
   ✅ 通过！所有30天的车型汇总都等于每日目标

✅ 测试2: 验证所有天的总量加起来 = 门店月度总目标
   ✅ 通过！月度汇总 = 月度目标 (500)

✅ 测试3: 验证各车型的月度总量
   各车型月度总量：
      - L6: 150 (30.00%)
      - L7: 200 (40.00%)
      - L9: 150 (30.00%)
   ✅ 通过！各车型月度总和 = 门店月度目标 (500)

✅ 测试4: 验证所有值都是非负整数
   ✅ 通过！所有值都是非负整数

[... 继续测试其他19个门店 ...]

============================================================
📋 测试结果汇总
============================================================

🎉 恭喜！所有测试全部通过！
✅ 门店数据分配完全正确
✅ 每天各车型汇总 = 每日目标
✅ 月度汇总 = 月度目标
✅ 所有值都是非负整数

========================================
🏁 测试完成
========================================
```

### 失败示例

如果测试失败，会显示类似：

```
❌ 测试失败！发现 5 个错误
   - 第3天: 目标=17, 实际=16, 差异=1
   - 第8天: 目标=20, 实际=21, 差异=-1
   ...
```

## 修复验证

修复后，应该能够满足：

1. ✅ **首次生成即正确**：不再需要手动调整
2. ✅ **每日汇总精确**：每天各车型加起来严格等于该天的目标总量
3. ✅ **月度汇总精确**：所有天的总量加起来严格等于门店月度总目标
4. ✅ **数据一致性**：所有值都是非负整数

## 常见问题

### Q1: 测试显示"尚未生成门店数据"？
**A**: 请确保已完成以下步骤：
1. 上传历史数据
2. 生成每日比例
3. 为所有车型设置月度目标
4. 选择区域
5. 分配门店目标并生成门店数据

### Q2: 如何手动验证某个门店？
**A**: 使用 `inspectStore("门店名称")` 查看详细数据表格，可以手动检查每天的车型汇总。

### Q3: 测试通过后还需要做什么？
**A**: 可以正常使用工具，导出Excel文件。导出的数据会包含修复后的正确分配。

## 文件修改清单

1. **store-allocation.js** (第394-512行)
   - 重写了 `generateStoreData()` 函数
   - 采用新的分配策略

2. **test-store-allocation.js** (新增)
   - 自动化测试脚本
   - 提供 `testStoreAllocation()` 和 `inspectStore()` 函数

3. **index.html** (第488行)
   - 添加了测试脚本的引用

## 技术细节

### 最大余数法（Largest Remainder Method）

修复后的代码在两个层面使用最大余数法：

1. **第一层**：分配门店总量到各天
   ```javascript
   // 确保所有天的总和 = 门店月度总目标
   targetDailyTotals[i] = floor(exactValue) + (hasRemainder ? 1 : 0)
   ```

2. **第二层**：对每一天，分配给各车型
   ```javascript
   // 确保该天各车型的总和 = 该天的目标总量
   storeVehicles[vehicle][day] = floor(exactValue) + (hasRemainder ? 1 : 0)
   ```

这种两层嵌套的最大余数法，确保了：
- 横向：每天各车型加起来 = 该天总量
- 纵向：所有天加起来 = 月度总目标

## 联系支持

如果测试失败或遇到问题，请提供：
1. 控制台的完整测试输出
2. 使用的测试数据（可脱敏）
3. 浏览器版本和操作系统信息
