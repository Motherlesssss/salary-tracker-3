# 交互流程设计文档

## 核心原则
1. 每个步骤有明确的前置条件和完成标志
2. 按钮的启用/禁用由状态统一管理
3. 用户始终清楚当前所在步骤和下一步操作
4. 步骤完成后自动引导到下一步

## 状态管理

```javascript
const workflow = {
    // 步骤完成状态
    regionSelected: false,         // 步骤1：区域已选
    dataUploaded: false,           // 步骤2：数据已上传
    vehiclesConfigured: false,     // 步骤2.5：车型已确认
    monthSelected: false,          // 步骤3：月份已选
    ratiosGenerated: false,        // 步骤5：比例已生成

    // 车型配置
    vehicleConfig: {},             // 车型详细配置
    enabledVehicleCount: 0,        // 启用的车型数量
};
```

## 完整流程

### 步骤1：选择区域
- **前置条件**：无
- **用户操作**：从下拉框选择区域
- **完成标志**：`workflow.regionSelected = true`
- **自动动作**：
  - 解锁步骤2（移除上传区域的disabled样式）
  - 显示提示："✓ 区域已选择，请上传历史数据"

### 步骤2：上传历史数据
- **前置条件**：`regionSelected = true`
- **用户操作**：上传Excel文件
- **完成标志**：`workflow.dataUploaded = true`
- **自动动作**：
  1. 解析数据，提取车型列表
  2. 初始化车型配置（所有车型默认启用）
  3. **自动显示**步骤2.5（车型管理面板）
  4. 滚动到车型管理区域
  5. 显示提示："✓ 数据已上传，检测到N个车型，请确认车型配置"

### 步骤2.5：车型管理（新增）
- **前置条件**：`dataUploaded = true`
- **显示时机**：数据上传后自动展开
- **用户操作**：
  1. 查看历史车型列表
  2. 可选：标记车型为"去库存"
  3. 可选：添加新车型
  4. 必须：点击"✓ 确认车型配置"按钮
- **完成条件**：`enabledVehicleCount >= 1`
- **完成标志**：`workflow.vehiclesConfigured = true`
- **确认按钮状态**：
  - 启用条件：`enabledVehicleCount > 0`
  - 禁用时显示：请至少启用一个车型
- **自动动作**：
  1. 隐藏车型管理面板
  2. 解锁步骤3（月份选择框可操作）
  3. 显示提示："✓ 车型配置已确认（N个车型），请选择预测月份"

### 步骤3：选择预测月份
- **前置条件**：`vehiclesConfigured = true`
- **用户操作**：选择年份和月份
- **完成标志**：`workflow.monthSelected = true`
- **自动动作**：
  1. 生成日历视图
  2. 解锁步骤5（生成按钮）
  3. 显示提示："✓ 月份已选择，可标记特殊节点（可选），然后点击生成"

### 步骤4：标记特殊节点（可选）
- **前置条件**：`monthSelected = true`
- **用户操作**：添加促销、调休等节点（可跳过）
- **完成标志**：无（可选步骤）

### 步骤5：生成分配比例
- **前置条件**：`vehiclesConfigured && monthSelected`
- **按钮状态**：
  - 启用条件：`workflow.vehiclesConfigured && workflow.monthSelected`
  - 禁用时显示：请完成前置步骤
- **用户操作**：点击"生成每日分配比例"按钮
- **完成标志**：`workflow.ratiosGenerated = true`
- **自动动作**：
  1. 显示loading
  2. 为每个车型生成比例：
     - 历史车型：使用历史算法
     - 新增车型：应用模板
     - 去库存车型：历史 × 衰减
  3. 隐藏loading
  4. 显示结果区域
  5. 默认切换到"历史车型汇总"标签
  6. 解锁导出按钮
  7. 滚动到结果区域

### 步骤6：查看和调整结果
- **前置条件**：`ratiosGenerated = true`
- **用户操作**：
  1. 切换标签页查看不同车型
  2. 在"历史车型汇总"拖拽调整
  3. 在新增/去库存车型单独调整
  4. 查看"总览"查看整体趋势
  5. 输入各车型月度目标
  6. 导出Excel
- **导出按钮状态**：
  - 启用条件：`workflow.ratiosGenerated = true`

## 按钮状态控制矩阵

| 操作 | 前置条件 | 禁用提示 |
|------|---------|---------|
| 上传数据 | regionSelected | 请先选择区域 |
| 添加新车型 | dataUploaded | 请先上传数据 |
| 确认车型配置 | dataUploaded && enabledVehicleCount > 0 | 请至少启用一个车型 |
| 选择月份 | vehiclesConfigured | 请先确认车型配置 |
| 生成比例 | vehiclesConfigured && monthSelected | 请完成车型配置和月份选择 |
| 导出Excel | ratiosGenerated | 请先生成分配比例 |

## 车型管理交互

### 添加新车型流程
1. 点击"➕ 添加新车型"按钮
2. 弹出模态框：
   ```
   车型代码：[____] （必填，如W03）
   车型名称：[____] （可选，如理想L6）

   节奏模板：
   ○ 平均模板（推荐）- 适用于大部分车型
   ○ i8模板（热度持久）- 适用于主力车型
   ○ MEGA模板（快速回落）- 适用于小众车型

   发布日期：[____] （必填，YYYY-MM-DD）

   [取消] [确认添加]
   ```
3. 点击"确认添加"后：
   - 验证输入（代码不能重复）
   - 添加到车型列表
   - 刷新车型管理界面
   - 更新enabledVehicleCount

### 标记去库存流程
1. 点击历史车型行的"标记去库存"按钮
2. 弹出模态框：
   ```
   车型：X01 理想ONE

   清库开始日期：[____] （如2025-03-20）
   清零日期：    [____] （如2025-03-31）

   说明：在此期间，商机量将线性衰减到0

   [取消] [确认]
   ```
3. 点击"确认"后：
   - 验证日期（开始 < 结束）
   - 更新车型类型为"clearStock"
   - 刷新车型管理界面
   - 行样式变为橙色，显示"📉"图标

### 启用/停用车型
- 历史车型默认启用
- 新增车型默认启用
- 点击"停用"按钮切换状态
- 停用时：
  - 按钮变为"启用"
  - 行样式变灰
  - enabledVehicleCount - 1
- 启用时：
  - 按钮变为"停用"
  - 行样式恢复
  - enabledVehicleCount + 1

## 标签页结构

生成比例后的标签页顺序（从左到右）：

1. **📊 历史车型汇总**
   - 显示条件：有历史车型且启用数量 >= 1
   - 内容：所有启用的历史车型汇总
   - 功能：可拖拽调整，联动分配

2. **W01、W02、...**（各历史车型）
   - 显示条件：车型启用
   - 内容：单个车型详情
   - 功能：查看（继承汇总调整结果）
   - 标记：去库存车型显示"📉"

3. **W03 🆕、W04 🆕、...**（新增车型）
   - 显示条件：车型启用
   - 内容：新增车型详情
   - 功能：可单独拖拽调整
   - 样式：蓝色背景

4. **📈 总览**
   - 显示条件：始终显示
   - 内容：所有启用车型的总和
   - 功能：只读，不可调整
   - 样式：灰色背景

## 视觉反馈

### 步骤指示器
每个步骤卡片显示状态：
- **未开始**：灰色数字，标题黑色
- **进行中**：蓝色数字，标题蓝色，卡片有蓝色边框
- **已完成**：绿色✓，标题变淡，卡片可折叠

### 提示信息
每个步骤完成后显示成功提示：
```html
<div class="success-hint">
  ✓ [步骤名称]已完成，[下一步操作提示]
</div>
```

### 禁用状态
按钮禁用时显示tooltip：
```html
<button disabled title="请先完成：选择区域、上传数据">
  生成每日分配比例
</button>
```

## 错误处理

### 车型管理
- 代码重复：提示"车型代码已存在"
- 代码为空：提示"请输入车型代码"
- 发布日期无效：提示"请输入有效日期"
- 全部停用：禁用"确认配置"按钮，提示"请至少启用一个车型"

### 生成比例
- 历史车型无数据：使用平均比例，显示警告
- 新增车型模板应用失败：回退到平均比例
- 去库存日期冲突：提示并要求修正

## 数据流

```
用户上传数据
  ↓
提取车型列表 → 初始化车型配置（所有启用）
  ↓
用户配置车型
  ↓
用户确认配置 → 锁定车型列表
  ↓
用户选择月份
  ↓
用户点击生成
  ↓
为每个车型生成比例：
  - 历史车型：历史算法 → state.results[vehicle]
  - 新增车型：模板应用 → state.results[vehicle]
  - 去库存车型：历史 × 衰减 → state.results[vehicle]
  ↓
显示结果（标签页）
  ↓
用户调整（拖拽）
  - 历史汇总：联动调整历史车型
  - 单车型：只调整该车型
  ↓
门店继承（自动）
  - 每个门店继承所有车型的最终比例
  ↓
导出Excel
```

## 实现检查清单

- [ ] 状态管理：workflow对象
- [ ] 步骤1：区域选择 + 解锁步骤2
- [ ] 步骤2：上传数据 + 自动显示车型管理
- [ ] 步骤2.5：车型管理UI + 添加/编辑/停用
- [ ] 步骤3：月份选择 + 解锁生成按钮
- [ ] 步骤5：生成逻辑（历史/模板/衰减）
- [ ] 步骤6：多标签页（汇总/单车型/总览）
- [ ] 按钮状态控制（checkWorkflowState函数）
- [ ] 视觉反馈（步骤指示器、提示、禁用态）
- [ ] 错误处理（验证、回退）
- [ ] 门店继承（保持不变）
