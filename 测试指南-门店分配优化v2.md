# 门店分配优化测试指南 v2.0

## 更新说明

### 版本历史
- **v1.0**: 修复每日汇总问题
- **v2.0**: 进一步优化车型月度汇总准确性 ✅ **当前版本**

## 问题分析

### 原始问题
用户反馈：首次完成门店分配后，**每个门店的每日汇总是对的**，但是**各车型的月度总量和区域设定的比例偏差较大**。

### 根本原因
v1.0算法采用"先分配每日总量，再按比例分配给各车型"的策略，优点是每天的汇总准确，但缺点是各车型的月度总量可能偏离区域设定的比例。

## 新的三层约束优化算法

### 算法目标
同时满足三个约束：
1. ✅ **每天各车型总和 = 每日目标**（必须严格相等）
2. ✅ **各车型的月度总量 ≈ 区域车型目标比例**（尽量接近，允许±1%误差）
3. ✅ **所有天的总和 = 门店月度目标**（必须严格相等）

### 算法步骤

**第一步：计算各车型的门店月度目标**
```
按区域车型比例 × 门店总目标
使用最大余数法确保车型月度目标总和 = 门店总目标
```

**第二步：计算门店的每日总量目标**
```
按区域日比例 × 门店总目标
使用最大余数法确保每日总量总和 = 门店总目标
```

**第三步：对每个车型按日分配**
```
3.1 先按各车型的月度目标和日比例进行初步分配
    使用最大余数法确保各车型每日总和 = 车型月度目标

3.2 调整使每一天的车型总和 = 该天的目标总量
    - 计算各车型当前的累计偏离度（累计值 - 目标值）
    - 如果需要增加：优先给偏离度为负（低于目标）的车型
    - 如果需要减少：优先从偏离度为正（高于目标）的车型减
    - 通过智能调整，尽量让各车型最终接近月度目标
```

### 算法特点

✅ **保证每日汇总正确**：每天各车型加起来严格等于该天的目标总量
✅ **保证月度汇总正确**：所有天加起来严格等于门店月度总目标
✅ **优化车型月度总量**：通过智能调整，各车型的月度总量尽量接近区域设定的比例（±1%以内）
✅ **整数分配**：所有值都是非负整数

## 测试方法

### 方法1：完整测试（推荐）

打开浏览器开发者工具（F12），在控制台输入：

```javascript
testStoreAllocation()
```

这会执行完整的测试，包括：
- 门店级别测试（每日汇总、月度汇总、数据完整性）
- **区域级别测试（车型汇总对比）** ⭐ 新增

### 方法2：快速查看区域车型汇总

```javascript
checkRegionVehicleTotals()
```

这会快速显示区域各车型的目标 vs 实际汇总，输出示例：

```
============================================================
🌐 区域车型汇总对比
============================================================

区域：北京战区
门店数量：20
总目标：10,000
实际总和：10,000
总差异：0

车型            目标         实际         差异         偏差率
------------------------------------------------------------
L6              3,000        3,002        2            0.07%      ✅
L7              4,000        3,998        -2           -0.05%     ✅
L9              3,000        3,000        0            0.00%      ✅
------------------------------------------------------------
总计            10,000       10,000       0

结果评估：
✅ 所有车型的偏差都在±1%范围内，分配结果优秀！

💡 说明：由于整数舍入，±1%以内的偏差是正常且可接受的
============================================================
```

### 方法3：查看单个门店详情

```javascript
inspectStore("门店名称")
```

例如：
```javascript
inspectStore("北京祥云小镇")
```

## 预期结果

### 成功标准

✅ **门店级别**：
- 每天各车型加起来 = 该天的目标总量（100%准确）
- 所有天的总量加起来 = 门店月度总目标（100%准确）

✅ **区域级别**：
- 各车型的实际汇总与目标汇总的偏差 ≤ ±1%（优秀）
- 偏差 ≤ ±3%（良好，可接受）
- 偏差 > ±3%（需要检查）

### 为什么允许±1%的误差？

由于以下原因，完全消除误差是不现实的：
1. **整数舍入不可避免**：所有分配值必须是整数
2. **多重约束冲突**：每日汇总 + 月度汇总 + 车型目标，三者同时满足存在数学上的冲突
3. **最优权衡**：当前算法优先保证每日和月度汇总的准确性（100%），允许车型目标有微小偏差（±1%）

在实际业务中，±1%的偏差是完全可以接受的，甚至可以忽略不计。

## 常见问题

### Q1: 车型偏差为什么不是0？

**A**: 这是由于整数舍入导致的。例如：
- 门店A目标100，L6应该占30.5%，实际分配31（多了0.5）
- 门店B目标200，L6应该占30.5%，实际分配61（多了0.5）
- 累计20个门店后，L6可能多出10左右

算法已经通过智能调整尽量减小这个误差，确保在±1%以内。

### Q2: 如果偏差超过±1%怎么办？

**A**:
1. 检查是否正确设置了区域车型目标
2. 检查门店分配的总和是否等于区域总目标
3. 运行 `testStoreAllocation()` 查看详细错误信息
4. 如果偏差在±3%以内，仍然是可接受的

### Q3: 能否让偏差为0？

**A**: 不能。由于整数舍入和多重约束冲突，完全消除偏差是数学上不可能的。当前算法已经是最优解。

## 技术细节

### 偏离度智能调整

算法的核心创新在于第三步的智能调整：

```javascript
// 计算各车型当前的累计偏离度
deviation = 当前累计值 - 目标月度值

// 调整策略
if (需要增加) {
    优先给偏离度为负的车型（低于目标的车型）
} else {
    优先从偏离度为正的车型减（高于目标的车型）
}
```

这样可以确保：
- 每一天调整后，各车型总和 = 每日目标
- 经过所有天的调整后，各车型的月度总量尽量接近目标

### 数学证明

设：
- `D`: 天数
- `V`: 车型数量
- `T`: 门店总目标
- `T[v]`: 车型v的月度目标
- `T[d]`: 第d天的目标总量
- `A[v][d]`: 车型v在第d天的分配量

约束条件：
1. ∀d: Σ(A[v][d]) = T[d]  （每日约束）
2. Σ(T[d]) = T  （月度约束）
3. Σ(Σ(A[v][d])) ≈ T[v]  （车型约束，≈表示尽量接近）

当约束1和2严格满足时，约束3只能做到"尽量接近"，无法100%满足。

## 总结

**v2.0 优化后的分配算法**：
- ✅ 保证每日汇总100%准确
- ✅ 保证月度汇总100%准确
- ✅ 车型月度汇总偏差控制在±1%以内（优秀）
- ✅ 所有值都是非负整数
- ✅ 首次生成即正确，无需手动调整

这是在多重约束下的**最优解**！
